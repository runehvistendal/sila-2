// Shared email template helpers — imported inline (copy into each function as needed)
// Since backend functions cannot import local files, this is a reference module.
// The actual helpers are inlined into each function.

export const SILA_URL = 'https://sila.gl';